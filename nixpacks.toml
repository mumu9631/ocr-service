[phases.setup]
nixPkgs = ["python310", "gcc", "libGL", "libglib", "zlib"]

[phases.build]
cmds = ["python -m venv /opt/venv", "/opt/venv/bin/pip install --upgrade pip", "/opt/venv/bin/pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
